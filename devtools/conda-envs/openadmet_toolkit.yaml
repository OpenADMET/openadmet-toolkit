name: openadmet_toolkit
channels:

  - defaults
dependencies:
    # Base depends
  - python >= 3.10
  - pip

  - jupyterlab
  - numpy
  - pandas
  - rdkit
  - cairosvg
  - python-pptx
  - seaborn
  - matplotlib
  - pydantic
  - duckdb

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pre-commit

    # Pip-only installs
  - pip:
    - chembl-downloader
