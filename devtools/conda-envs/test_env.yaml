name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pre-commit

    # Pip-only installs
  #- pip:
  #  - codecov
