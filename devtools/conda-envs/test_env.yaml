name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python >= 3.10
  - pip

  - jupyterlab
  - numpy
  - pandas
  - rdkit
  - cariosvg
  - python-pptx

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pre-commit


    # Pip-only installs
  #- pip:
  #  - codecov
